import StorefrontTutorial from '@/components/StorefrontTutorial';

export const metadata = { title: 'Partner Storefront Tutorial — AF Home' };

export default function StorefrontTutorialPage() {
  return <StorefrontTutorial />;
}
