[phases.setup]
nixPkgs = [
  "nodejs_22"
]

[phases.install]
cmds = [
  "npm exec --yes pnpm@10.0.0 -- install --frozen-lockfile"
]

[phases.build]
cmds = [
  "npm exec --yes pnpm@10.0.0 -- run build"
]

[start]
cmd = "npm exec --yes pnpm@10.0.0 -- run start"
